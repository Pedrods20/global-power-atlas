name: Forecast

# The historical dashboard remains monthly. This job is separate because a
# prospective forecast has to be issued on the market clock, then reconciled
# after the delivery price is observable.

on:
  schedule:
    # DE-LU's gate is 12:00 local market time on D-1: 10:00Z under CEST, 11:00Z
    # under CET. GitHub's scheduled workflows are best-effort, and the previous
    # 09:30Z cron left only thirty summer minutes of margin: its runs were
    # actually delivered at 14:22Z and 15:56Z, hours past the gate, so every one
    # failed with LateIssueError -- `gpa issue` correctly refusing to backdate a
    # forecast. These two slots leave roughly eight and four hours of margin
    # against the summer gate, and both run off the top of the hour, where the
    # scheduler is least contended. Nothing needed is unavailable this early:
    # D-1's own prices cleared in D-2's auction.
    #
    # Running twice a day is safe by design. The ledger keys an issue on
    # issued_at as well as zone, model, delivery date and hour, so the backstop
    # appends a second issue rather than colliding with the first, and
    # `ledger.canonical` selects the earliest complete, verified pre-gate issue
    # per model and delivery day. Every issue stays recorded either way.
    #
    # The two slots also serve the day-ahead fundamentals question. Those
    # features enter a prospective panel only when the provider has already
    # published the delivery day; checked at 05:45 CEST on 16 September 2026,
    # Energy-Charts had not, so an early run falls back to the published
    # information set and says so in its own manifest. The later slot has the
    # better chance of catching them. Which run wins the canonical slot is
    # deliberately not made to depend on that: the conservative, published
    # information set stays canonical, exactly as in the retrospective release,
    # and a fundamentals-bearing issue is recorded alongside it as evidence.
    - cron: "17 2 * * *"
    - cron: "47 6 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: forecast-issue
  cancel-in-progress: false

jobs:
  issue:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      # The public store and monthly site exports stay untouched. The forecast
      # run receives the committed history as a seed and catches it up from
      # the seed's own checkpoint before reconciliation and issuance, so a
      # stale monthly seed is recovered rather than capped at a fixed window.
      GPA_DATA_ROOT: /tmp/gpa-curated
      FORECAST_MODEL: ridge
      # Revision overlap behind that checkpoint, not the length of the refresh.
      FORECAST_LOOKBACK: "7"
      FORECAST_DELIVERY_DATE: ""

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - run: pip install -e .

      - name: Register this run as a tracked attempt before any refresh
        # A setup or ingestion failure below must still leave local evidence
        # that this scheduled slot was attempted, not silently absent.
        run: |
          ATTEMPT_ID="gh-${{ github.run_id }}-${{ github.run_attempt }}"
          echo "ATTEMPT_ID=$ATTEMPT_ID" >> "$GITHUB_ENV"
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            ORIGIN=workflow_dispatch
          else
            ORIGIN=schedule
          fi
          if [ -n "$FORECAST_DELIVERY_DATE" ]; then
            gpa forecast-attempt start --attempt-id "$ATTEMPT_ID" --zone DE-LU \
              --model "$FORECAST_MODEL" --origin "$ORIGIN" --delivery-date "$FORECAST_DELIVERY_DATE"
          else
            gpa forecast-attempt start --attempt-id "$ATTEMPT_ID" --zone DE-LU \
              --model "$FORECAST_MODEL" --origin "$ORIGIN"
          fi

      - name: Seed an isolated forecast store
        run: |
          mkdir -p "$GPA_DATA_ROOT"
          cp -a data/curated/. "$GPA_DATA_ROOT/"

      - name: Refresh the DE-LU forecast inputs
        # Prices are requested past now: D-1's whole curve is public before
        # this morning run, and the panel drops an incomplete D-1 entirely.
        run: gpa ingest --zone DE-LU --days "$FORECAST_LOOKBACK"

      - name: Validate the isolated inputs
        run: gpa validate

      - name: Reconcile previously issued forecasts
        run: gpa reconcile --zone DE-LU

      - name: Evaluate reconciled battery dispatch
        run: gpa battery --zone DE-LU --output data/forecast_issues/evaluations

      - name: Issue before the market gate
        # Reuses the attempt registered above; its delivery date and origin
        # carry over, so a late or failed issue still finalizes the same
        # tracked attempt instead of leaving it open.
        run: gpa issue --zone DE-LU --model "$FORECAST_MODEL" --attempt-id "$ATTEMPT_ID"

      - name: Record a failed attempt if an earlier step never completed it
        # gpa issue always finalizes its own attempt, so --if-open is a no-op
        # there; this only closes attempts left open by an earlier failure
        # (ingest, validate, reconcile or battery).
        if: failure()
        run: gpa forecast-attempt finish --attempt-id "$ATTEMPT_ID" --status failed --if-open

      - name: Commit the prospective ledger
        # Runs even after an earlier failure so failed-attempt evidence is
        # preserved; the job's own conclusion still reports that failure.
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/forecast_issues
          if git diff --cached --quiet; then
            echo "No new issue or attempt records; nothing to commit."
            exit 0
          fi
          git commit -m "forecast: issue DE-LU $(date -u +%Y-%m-%d)"
          git pull --rebase --autostash origin main
          git push
