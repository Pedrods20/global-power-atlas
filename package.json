{
  "name": "global-power-atlas-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static site for the DE-LU power market research note.",
  "scripts": {
    "dev": "observable preview",
    "build": "observable build",
    "test:browser": "node scripts/browser-smoke.mjs",
    "clean": "observable clean"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.0"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
